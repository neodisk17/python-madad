{
  "name": "python-helper",
  "displayName": "python-helper",
  "description": "",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "workspaceContains:requirements.txt",
    "onLanguage:pip-requirements"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "python-helper.downloadFile",
        "title": "Download File",
        "icon": {
          "dark": "./src/assets/icon/download-icon.svg",
          "light": "./src/assets/icon/download-icon.svg"
        }
      },
      {
        "command": "python-helper.install-python-package",
        "title": "Install Python Package"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "when": "resourceLangId == 'pip-requirements'",
          "command": "python-helper.downloadFile",
          "group": "navigation"
        }
      ],
      "editor/context": [
        {
          "command": "python-helper.downloadFile",
          "when": "resourceLangId == 'pip-requirements'"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "webpack",
    "watch": "webpack --watch",
    "package": "webpack --mode production --devtool hidden-source-map",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.6",
    "@types/node": "18.x",
    "@types/vscode": "^1.85.0",
    "@typescript-eslint/eslint-plugin": "^6.13.1",
    "@typescript-eslint/parser": "^6.13.1",
    "@vscode/test-cli": "^0.0.4",
    "@vscode/test-electron": "^2.3.8",
    "eslint": "^8.54.0",
    "ts-loader": "^9.5.1",
    "typescript": "^5.3.2",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "node-fetch": "^3.3.2",
    "node-html-parser": "^6.1.11"
  }
}
